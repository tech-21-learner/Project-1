# Project-1
1. Check for duplicate integers in 2D array. Helper function duplicates returns the number of integers which are duplicate(if any).{file = duplicate.cpp}
2. Check if user provided array is palindrom or not.{file = isPalindrom.cpp}
3. A function reverseDigit takes an integer as a parameter and it returns the reverse digit.{file = reverse.cpp}
